<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Créditos</title>
    <link rel="stylesheet" href="css/estilos.css" type="text/css">
    <link rel="stylesheet" href="css/all.min.css" type="text/css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400&family=Roboto:wght@500&family=Ubuntu:wght@300&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="/Imagenes/favicon.ico" type="image/x-icon">
</head>
<body>
    <% if (logincli) { %>
    <header class="header">
        <div class="menu-princ contenedor">
            <a href="/logout" class="logout">Cerrar Sesión</a>
            <div class="btn-menu cont">
                <a href="#">
                    <i class="fas fa-user-circle"></i>
                </a>
            </div>
            <div class="usrn">
                <a href="/personal">
                    <p><%= name %></p>
                </a>
            </div>
        </div>
    </header>

    <section class="text">
        <div class="menuctx contenedor">
            <h2><i class="fas fa-address-book"></i>Página de Créditos</h2>
            <p>Visualiza tus créditos activos</p>
        </div>
    </section>

    <section class="form">
        <div class="tabla0">
            <table class="tabla contenedor">
                <thead>
                    <tr>
                        <th>N.o</th>
                        <th>Código</th>
                        <th>Documento</th>
                        <th>Código de Línea</th>
                        <th>Monto Prestado</th>
                        <th>Fecha Aprobado</th>
                        <th>Plazo</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (data) { %>
                        <% for (var i = 0; i < data.length; i++) { %>
                            <tr>
                                <td><%= (i + 1) %></td>
                                <td><%= data[i].CodigoCredito %></td>
                                <td><%= data[i].DocCli %></td>
                                <td><%= data[i].CodLinea %></td>
                                <td><%= data[i].MontoPrestado %></td>
                                <td><%= data[i].FechaAproba %></td>
                                <td><%= data[i].Plazo %></td>
                            </tr>
                        <% } %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </section>

    <div class="cont-menu">
        <nav>
            <p><i class="fas fa-university"></i></p>
            <a href="/lineascli">Líneas de Crédito</a>
            <a href="/creditoscli">Créditos</a>
            <a href="/cuentascli">Cuentas</a>
            <a href="/operaciones">Operaciones</a>
            <div class="copy1">
                <p>Oceanic Bank</p>
                <p>Copyright 2021</p>
            </div>
        </nav>
    </div>
    <% } else { %>
        <h1><strong><%= name %></strong></h1>
        <a href="login" class="a-login">Ir al Login</a>
    <% } %>
</body>
</html>